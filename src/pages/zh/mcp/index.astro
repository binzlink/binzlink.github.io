---
import ListPage from '../../../components/ListPage.astro';
import { itemsByType } from '../../../lib/catalog';

const items = await itemsByType('mcp');
---

<ListPage locale="zh" type="mcp" items={items} />
